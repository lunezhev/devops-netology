# devops-netology
## Домашнее задание к занятию "5.1. Введение в виртуализацию. Типы и функции гипервизоров. Обзор рынка вендоров и областей применения."

```
1. Основное отличие полной (аппаратной) виртуализации, паравиртуализации и виртуализации на основе ОС в том, что в первых двух ресурсами "рулит" гипервизор, а в третьем - сама ОС, а разница между первым и вторым в том, что при полной виртуализации гипервизор - суть есть сам себе ОС, а при паравиртуализации гипервизору необходима ОС для доступа к ресурсам. также при вертуализации уровня ОС не запустить гостевую ОС, чье ядро отличается от ядра хостовой ОС. 


2. Высоконагруженная база данных, чувствительная к отказу: Лучше bare-metall))), т.к. нужна наибольшая производительность, а за счет отсутствия гипервизора также повышается отказоустойчивость.
   
   Различные Java-приложения: виртуализация уровня ОС (контейнеризация), т.к. требуется меньше ресурсов как для приложений, так и для администрирования, простота масштабирования.

   Windows системы для использования Бухгалтерским отделом: паравиртуализация, т.к. ресурсов много не требуют, но возникают требования к присутствию различных ОС, а также удобство бэкапирования и управлению ресурсами самих виртуальных машин ("мала жесткаго диска, апператифки нихватаит")

   Системы, выполняющие высокопроизводительные расчеты на GPU: Возможно, подойдет паравиртуализация, чтобы GPU считала основная ОС на чистом железе, а пользователь гостевой ОС получал готовые результаты для дальнейшей обработки.
   

3. Сценарии:

1) 100 виртуальных машин на базе Linux и Windows, общие задачи, нет особых требований. Преимущественно Windows based инфраструктура, требуется реализация программных балансировщиков нагрузки, репликации данных и автоматизированного механизма создания резервных копий.
   ОТВЕТ: VM-Ware, с самого начала заточен под десктопную виртуализацию, поддерживает все требования.

2) Требуется наиболее производительное бесплатное open source решение для виртуализации небольшой (20-30 серверов) инфраструктуры на базе Linux и Windows виртуальных машин.
   ОТВЕТ: ProxMox. Бесплатная, высокая производительность и надежность, резервное копирование, легкое управление.

3) Необходимо бесплатное, максимально совместимое и производительное решение для виртуализации Windows инфраструктуры.
   ОТВЕТ: microsoft hyper-v, так как win-инфраструктура, то разработка от микрософт будет самая подходящая.

4) Необходимо рабочее окружение для тестирования программного продукта на нескольких дистрибутивах Linux.
   ОТВЕТ: Docker, тестирование ПО не требует оч высокой нагрузки, легкая масштабируемость, управление версиями разрабатываемого ПО в контейнерах, легкая настройка с системами разработки и git-системами.

```
microsoft hyper-v: разработка микрософт, полностью удовлетворяет требованиям: "реализация программных балансировщиков нагрузки, репликации данных и автоматизированного механизма создания резервных копий", низкий порог входа, т.к. вряд ли на 100 машин будут содержать штат высококвалифицированных админов.